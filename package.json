{
  "name": "bridgeapi",
  "version": "0.1.0",
  "private": true,
  "description": "BridgeAPI — MCP connectors ecosystem for Brazilian platforms",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "author": "BridgeAPI <contato@bridgeapi.com.br>",
  "license": "MIT"
}
